Read docs/CLASSLENS_IMPLEMENTATION_PLAN.md — we're doing Session $ARGUMENTS now. Start with
git status --short and confirm branch. Follow that session's scope from the plan exactly:
don't touch work outside it, don't skip its exit condition, ask before any deploy/push/remote
change. When done, report what's verified (with evidence), what's not, and what the next
session needs from you.
